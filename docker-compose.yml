services:
  postgres:
    image: pgvector/pgvector:pg17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: xhschat
      POSTGRES_PASSWORD: xhschat_dev
      POSTGRES_DB: xhschat
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
