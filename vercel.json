{
  "framework": null,
  "installCommand": "npm install && npx prisma generate && cd frontend && npm install",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/health", "destination": "/api" },
    { "source": "/((?!api|health|assets|icon-|manifest).*)", "destination": "/index.html" }
  ]
}
